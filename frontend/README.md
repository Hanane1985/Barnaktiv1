Frontend application for Barnaktiv.

The active app lives in `web`.

## Commands

```powershell
cd web
npm ci
npm run dev
npm run lint
npm run build
```

## Configuration

Set `BARNAKTIV_API_BASE_URL` to the backend API base URL in staging and production. See `web/.env.example`.
