# Server-side API URL used by Next.js when rendering activities.
BARNAKTIV_API_BASE_URL=https://api-domain.com

# Only set this if browser-side code needs the API URL in the future.
# NEXT_PUBLIC_BARNAKTIV_API_BASE_URL=https://your-api-domain.example
